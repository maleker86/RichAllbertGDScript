@tool
extends EditorScript

var holder: String = "name"

# Called when the script is executed (using File -> Run in Script Editor).
func _run() -> void:
	pass

# i know how if and elif and else work so
# i will just note break and continue as a reminder to use them
